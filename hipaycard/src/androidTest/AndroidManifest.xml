<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The one-click decline tests exercise the REAL pay path against the gateway (an
         unauthenticated call — any failure class maps to the same observable outcome). -->
    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
